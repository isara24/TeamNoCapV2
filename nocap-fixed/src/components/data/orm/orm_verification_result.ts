// Code generated by raf using Material interface.
// - Namespace: 01987547fc6c72ecb453bd2736bd4ea0.
// - Entity Name: verification_result.
// - Entity ID: 102019a1d99a4037459b78fa93ee571b519.
// - Entity Version: 102019a1dbcbce7723a8fcbc902bf548690.
// - Task ID: 68fd5f5b822475ee4330a749.
// - Task Root ID: 68fd5593b7797f696b910611.

import { DataType } from "./common";
import type { Value, Page, Index, Filter, Sort, Data } from "./common";
import { DataStoreClient, CreateData, CreateValue, ParseValue } from "./client";










/**
 * Interface for VerificationResultModel
 * `id`, `data_creator`, `data_updater`, `create_time`, `update_time` should not be set when INSERTING data since backend will fill it automatically.
 * `id` should keep the original value when setting (updating) data.
 * `data_creator` and `data_updater` are read-only strings (user id) representing the creator and updater of the data record.
 * `create_time` and `update_time` are read-only strings in the format of timestamp (10-digit).
 */
export interface VerificationResultModel {
  id: string;
  data_creator: string;
  data_updater: string;
  create_time: string;
  update_time: string;
  declarative_statement_id: string;
  claude_verification?: string | null;
  fetch_ai_verification?: string | null;
  gemini_verification?: string | null;
  bright_data_verification?: string | null;
  lava_gateway_consensus?: string | null;
  is_false_statement: boolean;
  correct_information?: string | null;
}

/**
 * ORM class for VerificationResult entity.
 * It uses Singleton pattern to prevent re-initialization.
 */
export class VerificationResultORM {
  private static instance: VerificationResultORM | null = null;
  protected client: DataStoreClient;
  protected namespace: string;
  protected entityId: string;
  protected entityName: string;
  protected entityVersion: string;
  protected taskId: string;
  protected taskRootId: string;

  private constructor() {
    this.client = DataStoreClient.getInstance();
    this.namespace = '01987547fc6c72ecb453bd2736bd4ea0';
    this.entityId = '102019a1d99a4037459b78fa93ee571b519';
    this.entityName = 'verification_result';
    this.entityVersion = '102019a1dbcbce7723a8fcbc902bf548690';
    this.taskId = '68fd5f5b822475ee4330a749';
    this.taskRootId = '68fd5593b7797f696b910611';
  }

  /**
   * Get singleton instance of VerificationResultORM
   */
  public static getInstance(): VerificationResultORM {
    if (!VerificationResultORM.instance) {
      VerificationResultORM.instance = new VerificationResultORM();
    }
    
    return VerificationResultORM.instance;
  }

  /**
   * Get all VerificationResult records
   * This returns all data without pagination.
   * To query data with page and filter conditions, use `list*()` instead.
   */
  async getAllVerificationResult(): Promise<VerificationResultModel[]> {
    const response = await this.client.all({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      format: {
        structured: true
      }
    });
    return this.resultToData(response.data?.values || []);
  }

  /**
   * Insert (create) new VerificationResult record(s)
   * DO NOT SET `id`, `data_creator`, `data_updater`, `create_time` and `update_time` since backend will fill it automatically.
   * It will respond the inserted record(s), with `id`, `data_creator`, `data_updater`, `create_time` and `update_time` filled by backend.
   */
  async insertVerificationResult(data: VerificationResultModel[]): Promise<VerificationResultModel[]> {
    const structured = data.map((item) => CreateData(VerificationResultModelToValues(item)));
    const response = await this.client.insert({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      batch: structured,
      format: {
        structured: true
      }
    });
    return this.resultToData(response.data?.values || []);
  }

  /**
   * Purge all VerificationResult records
   */
  async purgeAllVerificationResult(): Promise<void> {
    await this.client.purge({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      format: {
        structured: true
      }
    });
  }

  /**
   * List VerificationResult records with filters
   * This function provides search capabilities like filtering, sorting, pagination.
   */
  async listVerificationResult(filter?: Filter, sort?: Sort, paginate?: Page): Promise<[VerificationResultModel[], Page]> {
    const response = await this.client.list({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      filter: filter,
      sort: sort,
      paginate: paginate,
      format: {
        structured: true
      }
    });
    return [this.resultToData(response.data?.values || []), response.data?.page || { number: 0, size: 0 }];
  }


  /**
   * Get verification_result by Id index
   * This function gets data by index.
   */
  async getVerificationResultById(
    id: string,
  ): Promise<VerificationResultModel[]> {
    const index = createIndexId(
      id
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) verification_result by Id index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setVerificationResultById(
    id: string,
    data: VerificationResultModel
  ): Promise<VerificationResultModel[]> {
    const index = createIndexId(
      id
    );

    const values = VerificationResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete verification_result by Id index
   */
  async deleteVerificationResultById(
    id: string
  ): Promise<void> {
    const index = createIndexId(
      id
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }
  /**
   * Get verification_result by DataCreator index
   * This function gets data by index.
   */
  async getVerificationResultByDataCreator(
    data_creator: string,
  ): Promise<VerificationResultModel[]> {
    const index = createIndexDataCreator(
      data_creator
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) verification_result by DataCreator index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setVerificationResultByDataCreator(
    data_creator: string,
    data: VerificationResultModel
  ): Promise<VerificationResultModel[]> {
    const index = createIndexDataCreator(
      data_creator
    );

    const values = VerificationResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete verification_result by DataCreator index
   */
  async deleteVerificationResultByDataCreator(
    data_creator: string
  ): Promise<void> {
    const index = createIndexDataCreator(
      data_creator
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }
  /**
   * Get verification_result by DataUpdater index
   * This function gets data by index.
   */
  async getVerificationResultByDataUpdater(
    data_updater: string,
  ): Promise<VerificationResultModel[]> {
    const index = createIndexDataUpdater(
      data_updater
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) verification_result by DataUpdater index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setVerificationResultByDataUpdater(
    data_updater: string,
    data: VerificationResultModel
  ): Promise<VerificationResultModel[]> {
    const index = createIndexDataUpdater(
      data_updater
    );

    const values = VerificationResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete verification_result by DataUpdater index
   */
  async deleteVerificationResultByDataUpdater(
    data_updater: string
  ): Promise<void> {
    const index = createIndexDataUpdater(
      data_updater
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }
  /**
   * Get verification_result by DeclarativeStatementId index
   * This function gets data by index.
   */
  async getVerificationResultByDeclarativeStatementId(
    declarative_statement_id: string,
  ): Promise<VerificationResultModel[]> {
    const index = createIndexDeclarativeStatementId(
      declarative_statement_id
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) verification_result by DeclarativeStatementId index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setVerificationResultByDeclarativeStatementId(
    declarative_statement_id: string,
    data: VerificationResultModel
  ): Promise<VerificationResultModel[]> {
    const index = createIndexDeclarativeStatementId(
      declarative_statement_id
    );

    const values = VerificationResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete verification_result by DeclarativeStatementId index
   */
  async deleteVerificationResultByDeclarativeStatementId(
    declarative_statement_id: string
  ): Promise<void> {
    const index = createIndexDeclarativeStatementId(
      declarative_statement_id
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }
  /**
   * Get verification_result by IsFalseStatement index
   * This function gets data by index.
   */
  async getVerificationResultByIsFalseStatement(
    is_false_statement: boolean,
  ): Promise<VerificationResultModel[]> {
    const index = createIndexIsFalseStatement(
      is_false_statement
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) verification_result by IsFalseStatement index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setVerificationResultByIsFalseStatement(
    is_false_statement: boolean,
    data: VerificationResultModel
  ): Promise<VerificationResultModel[]> {
    const index = createIndexIsFalseStatement(
      is_false_statement
    );

    const values = VerificationResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete verification_result by IsFalseStatement index
   */
  async deleteVerificationResultByIsFalseStatement(
    is_false_statement: boolean
  ): Promise<void> {
    const index = createIndexIsFalseStatement(
      is_false_statement
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }

  /**
   * Convert result data to VerificationResultModel data array
   */
  private resultToData(values: Data[]): VerificationResultModel[] {
    return values.map((item: Data) => {
      if (item.structured && item.structured.length > 0) {
        return VerificationResultModelFromValues(item.structured);
      }

      if (item.serialized) {
        try {
          const parsedData = JSON.parse(item.serialized) as VerificationResultModel;
          return parsedData;
        } catch (error) {
          console.error('Error parsing serialized VerificationResultModel data: ', error, item.serialized);
          return null;
        }
      }

      return null;
    }).filter((item): item is VerificationResultModel => item !== null);
  }
}

/**
 * Convert VerificationResultModel data to Value array
 */
function VerificationResultModelToValues(data: VerificationResultModel): Value[] {
  const fieldMappings = [
    { key: 'id', type: DataType.string, defaultValue: '' },
    { key: 'data_creator', type: DataType.string, defaultValue: '' },
    { key: 'data_updater', type: DataType.string, defaultValue: '' },
    { key: 'create_time', type: DataType.string, defaultValue: '' },
    { key: 'update_time', type: DataType.string, defaultValue: '' },
    { key: 'declarative_statement_id', type: DataType.string, defaultValue: '' },
    { key: 'claude_verification', type: DataType.string, defaultValue: null },
    { key: 'fetch_ai_verification', type: DataType.string, defaultValue: null },
    { key: 'gemini_verification', type: DataType.string, defaultValue: null },
    { key: 'bright_data_verification', type: DataType.string, defaultValue: null },
    { key: 'lava_gateway_consensus', type: DataType.string, defaultValue: null },
    { key: 'is_false_statement', type: DataType.boolean, defaultValue: false },
    { key: 'correct_information', type: DataType.string, defaultValue: null },
  ];

  return fieldMappings.map(({ key, type, defaultValue }) => {
    const value = data[key as keyof VerificationResultModel] ?? defaultValue;
    return CreateValue(type, value, key);
  });
}

/**
 * Convert Value array to VerificationResultModel data
 */
function VerificationResultModelFromValues(values: Value[]): VerificationResultModel {
  const data: Partial<VerificationResultModel> = {};
  
  for (const value of values) {
    if (!value.name) continue;
    
    switch (value.name) {
      case 'id':
        data.id = ParseValue(value, DataType.string) as string;
        break;
      case 'data_creator':
        data.data_creator = ParseValue(value, DataType.string) as string;
        break;
      case 'data_updater':
        data.data_updater = ParseValue(value, DataType.string) as string;
        break;
      case 'create_time':
        data.create_time = ParseValue(value, DataType.string) as string;
        break;
      case 'update_time':
        data.update_time = ParseValue(value, DataType.string) as string;
        break;
      case 'declarative_statement_id':
        data.declarative_statement_id = ParseValue(value, DataType.string) as string;
        break;
      case 'claude_verification':
        data.claude_verification = ParseValue(value, DataType.string) as string | null;
        break;
      case 'fetch_ai_verification':
        data.fetch_ai_verification = ParseValue(value, DataType.string) as string | null;
        break;
      case 'gemini_verification':
        data.gemini_verification = ParseValue(value, DataType.string) as string | null;
        break;
      case 'bright_data_verification':
        data.bright_data_verification = ParseValue(value, DataType.string) as string | null;
        break;
      case 'lava_gateway_consensus':
        data.lava_gateway_consensus = ParseValue(value, DataType.string) as string | null;
        break;
      case 'is_false_statement':
        data.is_false_statement = ParseValue(value, DataType.boolean) as boolean;
        break;
      case 'correct_information':
        data.correct_information = ParseValue(value, DataType.string) as string | null;
        break;
    }
  }
  
  return data as VerificationResultModel;
}


/**
 * Create index for Id fields
 */
function createIndexId(
  id: string
): Index {
  const values: Value[] = [
    CreateValue(DataType.string, id, 'id'),
  ];

  return {
    fields: ['id'],
    values
  };
}
/**
 * Create index for DataCreator fields
 */
function createIndexDataCreator(
  data_creator: string
): Index {
  const values: Value[] = [
    CreateValue(DataType.string, data_creator, 'data_creator'),
  ];

  return {
    fields: ['data_creator'],
    values
  };
}
/**
 * Create index for DataUpdater fields
 */
function createIndexDataUpdater(
  data_updater: string
): Index {
  const values: Value[] = [
    CreateValue(DataType.string, data_updater, 'data_updater'),
  ];

  return {
    fields: ['data_updater'],
    values
  };
}
/**
 * Create index for DeclarativeStatementId fields
 */
function createIndexDeclarativeStatementId(
  declarative_statement_id: string
): Index {
  const values: Value[] = [
    CreateValue(DataType.string, declarative_statement_id, 'declarative_statement_id'),
  ];

  return {
    fields: ['declarative_statement_id'],
    values
  };
}
/**
 * Create index for IsFalseStatement fields
 */
function createIndexIsFalseStatement(
  is_false_statement: boolean
): Index {
  const values: Value[] = [
    CreateValue(DataType.boolean, is_false_statement, 'is_false_statement'),
  ];

  return {
    fields: ['is_false_statement'],
    values
  };
}

export default VerificationResultORM;